export const metadata = {
    title: "Register",
    description: "Create your LMS University account and start learning today.",
};

export default function RegisterLayout({ children }) {
    return children;
}
