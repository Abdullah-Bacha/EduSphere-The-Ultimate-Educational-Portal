export const metadata = {
    title: "Login",
    description: "Sign in to your LMS University account to access your courses and dashboard.",
};

export default function LoginLayout({ children }) {
    return children;
}
