export const metadata = {
    title: "Forgot Password",
    description: "Reset your LMS University account password.",
};

export default function ForgotPasswordLayout({ children }) {
    return children;
}
